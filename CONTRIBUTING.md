# 参与 AERMOD 中文文档维护

感谢参与 AERMODDoc。项目当前已完成 EPA 2023 年 10 月版 AERMOD 用户指南的中文在线整理，贡献重点包括翻译纠错、技术表述核对、版本更新、示例完善和网站可用性改进。

## 报告问题

提交 Issue 时，请尽量提供：

1. 页面地址或仓库文件路径；
2. 章节、小节、表格或关键字名称；
3. 当前中文表述；
4. 建议修改内容；
5. 英文原文页码、EPA 文件或其他依据；
6. 浏览器和设备信息（仅网站显示问题需要）。

## 翻译和术语原则

- 说明文字使用简体中文；
- AERMOD、AERMET、AERMAP、SCRAM、NAAQS、PSD 等专有名称和缩写保留英文；
- 路径 ID、关键字、次级关键字、参数、文件名、变量名、Fortran 格式和模型原始输出保留英文；
- 公式、单位、章节编号和交叉引用原则上保持原文；
- 全站优先使用“输入控制文件”“污染源组”“受体”“气象廓线”和“建筑物下洗”；
- 对法规状态、版本行为或参数限制的修改必须提供可靠依据；
- 法规应用说明不得替代 EPA 英文原文或主管机构要求。

## 本地开发

```bash
npm ci
npm run docs:dev
```

提交前执行：

```bash
npm run docs:check
npm run docs:build
npm run docs:check-links
```

全部命令必须通过。新增完整章节、附录、表格或关键字页面时，应同步增加或更新相应防退化检查。

## 文档结构

- `docs/guide/`：前置部分及第 1—4 章；
- `docs/appendices/`：附录 A—E；
- `docs/.vuepress/guide-sidebar.js`：用户指南侧栏；
- `docs/.vuepress/appendix-sidebar.js`：附录侧栏；
- `scripts/`：完整性、术语、交叉引用和构建后链接检查。

侧栏应采用“章—节—小节”结构。不要使用“2.2—2.3”“1—6”等区间式目录代替真实小节。

## Pull Request 要求

- 一个 PR 尽量只处理一个明确主题；
- PR 标题简洁说明修改范围；
- PR 正文列出修改内容、原文范围和验证方式；
- 不应删除原始关键字、参数、示例或稳定锚点；
- 修改页面标题或锚点时，必须同步调整侧栏和交叉引用；
- 不提交 `node_modules/`、构建产物或临时诊断文件。

## 许可证

提交贡献即表示同意按照仓库的 MIT License 发布相关修改。EPA 原始资料的使用仍应遵守其适用条款和法规要求。
